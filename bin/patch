#!/bin/sh
set -eu
tmp=$(mktemp)
guix shell libxslt \
     -- xsltproc \
     --output "$tmp" \
     libexec/patch.xsl \
     CommonMark.gen.rng
guix shell libxml2 \
     -- xmllint \
     --output CommonMark.rng \
     --format \
     "$tmp"
